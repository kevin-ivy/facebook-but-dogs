<div class="reviews mt-3">
    {{#each this}}
    <section class="review">
        <div class="meta">
        {{user.username}} on {{format_date created_at}}
        </div>
        <div class="text">
        {{review_text}}
        </div>
    </section>
    {{/each}}
</div>
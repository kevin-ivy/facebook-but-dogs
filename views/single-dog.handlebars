{{> post-info post }}
{{#if loggedIn}}
<form class="comment-form">
    <div>
        <textarea name="comment-body"></textarea>
    </div>

    <div>
        <button type="submit" class="btn btn-success">Add comment</button>
        <button type="button" class="upvote-btn btn btn-success">Give a dog a <i class="fas fa-bone"></i> !</button>
    </div>
    </form>
{{/if}}
{{> comments post.comments}}    

{{#if loggedIn}}
<script src="/javascript/review.js"></script> <!--change -->
<script src="/javascript/upbone.js"></script><!--change -->
{{/if}}